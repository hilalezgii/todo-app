import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        backgroundColor: '#9acd32',
        marginBottom: 10,
        padding: 10,
        borderRadius: 10,
    },
    text: {
        color: 'white',
    },
});